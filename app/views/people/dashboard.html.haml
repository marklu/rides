- content_for :dashboard_navigation do
  %li{:class => 'current'}
    != link_to 'Dashboard', person_root_path, :id => 'dashboard'
    %ul
      %li!= link_to 'Create Trip', new_trip_path
%h2 Upcoming Trips
!= render :partial => 'trips/trip', :collection => @upcoming_trips
