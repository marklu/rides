%h2 Ride Arrangements
- if @arrangements.count > 0
  - @arrangements.each do |arrangement|
    .section
      %h3 #{arrangement.driver.name}'s #{arrangement.vehicle.make} #{arrangement.vehicle.model}
      %dl
        %dt Driver
        %dd= arrangement.driver.name
        %dt Passengers
        %dd= arrangement.passengers.map {|passenger| passenger.name}.join(', ')
- else
  %p This trip has no arrangements yet. 
!= form_tag(generate_trip_arrangements_path(@trip), :method => :post) do
  %fieldset.embedded
    %h3 Submit
    != submit_tag 'Generate Arrangements'